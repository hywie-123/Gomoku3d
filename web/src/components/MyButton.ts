import styled from "@emotion/styled";
import { Button } from "@mui/material";

export const MyButton = styled(Button)({
    height: 28,
    padding: "0 14px",
    fontSize: '1rem',
    borderRadius: 14,
    textTransform: 'none',
    verticalAlign: 'baseline',
});

export const MyLargeButton = styled(Button)({
    height: 28,
    padding: "0 14px",
    fontSize: '1rem',
    borderRadius: 14,
    textTransform: 'none',
    verticalAlign: 'baseline',
});
